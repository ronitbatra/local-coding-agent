{
  "name": "@local-agent/core",
  "version": "0.1.0",
  "description": "Core engine for the local coding agent (no UI)",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "format": "biome format --write .",
    "format:check": "biome format .",
    "clean": "rm -rf dist"
  },
  "dependencies": {},
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
